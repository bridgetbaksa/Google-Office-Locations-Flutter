name: maps_poc_flutter
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  google_maps_flutter: ^0.5.21+15
  http: ^0.12.0+4
  json_serializable: ^3.2.5

dev_dependencies:
  pedantic: ^1.9.0
  build_runner: ^1.7.3

flutter:
  uses-material-design: true

